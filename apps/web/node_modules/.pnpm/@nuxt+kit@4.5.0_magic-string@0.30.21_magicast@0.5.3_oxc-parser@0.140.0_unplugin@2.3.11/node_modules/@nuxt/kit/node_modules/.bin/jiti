#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
else
  exec node  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
fi
# cmd-shim-target=/Users/nikolay/Documents/code/gonow/apps/web/node_modules/.pnpm/@nuxt+kit@4.5.0_magic-string@0.30.21_magicast@0.5.3_oxc-parser@0.140.0_unplugin@2.3.11/node_modules/jiti/lib/jiti-cli.mjs
