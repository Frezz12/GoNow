#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vue-demi/bin/vue-demi-switch.js" "$@"
else
  exec node  "$basedir/../../../../vue-demi/bin/vue-demi-switch.js" "$@"
fi
# cmd-shim-target=/Users/nikolay/Documents/code/gonow/apps/web/node_modules/.pnpm/@floating-ui+vue@1.1.11_vue@3.5.40_typescript@5.8.3_/node_modules/vue-demi/bin/vue-demi-switch.js
