# API contracts

The Rust backend publishes its machine-readable OpenAPI contract at `/api/openapi.json`.
Download a snapshot after starting the backend:

```bash
curl http://localhost:8080/api/openapi.json -o openapi.json
```
